# ============================================
# LIGHTWEIGHT CORE API DEPENDENCIES
# Total size: ~50-80MB
# Used by: API service (always running)
# ============================================

# FastAPI Core (Lightweight web framework)
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.6.0
pydantic-settings==2.1.0

# Database (Async SQLite support)
sqlalchemy==2.0.25
alembic==1.13.1
aiosqlite>=0.19.0

# HTTP Clients (Lightweight, no heavy parsing)
httpx==0.27.0
aiohttp==3.9.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
python-multipart==0.0.6
PyYAML==6.0.1

# WebSocket support
websockets==12.0

# Production utilities (minimal)
psutil==5.9.8

# ============================================
# EXCLUDED FROM CORE (Moved to Worker):
# ============================================
# ❌ playwright (200MB+)
# ❌ pandas (100MB+)
# ❌ trafilatura (50MB+)
# ❌ beautifulsoup4, lxml, selectolax
# ❌ openpyxl (Excel export)
# ❌ requests (use httpx instead)
# ❌ html2text
# ❌ fake-useragent
# ❌ deepdiff
# ❌ robotexclusionrulesparser
